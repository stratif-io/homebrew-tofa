# homebrew-tofa

Homebrew tap for [tofa](https://github.com/stratif-io/tofa) — eye-candy terminal OTP manager.

## Install

```bash
brew tap stratif-io/tofa
brew install tofa          # CLI formula
brew install --cask tofa   # macOS .app
```
